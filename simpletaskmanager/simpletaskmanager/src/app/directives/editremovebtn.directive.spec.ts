import { EditRemovebtn } from './editremovebtn.directive';

describe('EditRemovebtn', () => {
  it('should create an instance', () => {
    const directive = new EditRemovebtn();
    expect(directive).toBeTruthy();
  });
});
