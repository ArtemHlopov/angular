import { EnterbtnDirective } from './enterbtn.directive';

describe('EnterbtnDirective', () => {
  it('should create an instance', () => {
    const directive = new EnterbtnDirective();
    expect(directive).toBeTruthy();
  });
});
