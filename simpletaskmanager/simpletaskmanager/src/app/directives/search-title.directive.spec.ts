import { SearchTitleDirective } from './search-title.directive';

describe('SearchTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchTitleDirective();
    expect(directive).toBeTruthy();
  });
});
