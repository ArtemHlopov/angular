import { IpvDirective } from './ipv.directive';

describe('IpvDirective', () => {
  it('should create an instance', () => {
    const directive = new IpvDirective();
    expect(directive).toBeTruthy();
  });
});
