export enum Numbers {
  zero = 0,
  one,
  two,
  three,
  four,
  five,
  six,
  seven,
  eight,
  nine,
}

export enum Operation {
  plus = '+',
  minus = '-',
  divide = '/',
  mult = '*',
  equal = '=',
  reset = 'reset',
}

export const numAfterDot = 2;

export const ZeroValue = {
  val: ['0', '-0'],
  msg: 'На ноль делить нельзя',
};
